---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArtigoFrameworksAgentesPage from '../../components/pages/ArtigoFrameworksAgentesPage.jsx';
import { getCollection } from 'astro:content';

export function getStaticPaths() {
  return getCollection('articles').then((entries) =>
    entries.map((entry) => ({
      params: { slug: entry.id },
      props: { entry },
    })),
  );
}

const { entry } = Astro.props;
const { title, description, image } = entry.data;
---

<BaseLayout title={`${title} - Tech Briefing`} description={description} image={image} active="Artigo" type="article">
  <ArtigoFrameworksAgentesPage entry={{ slug: entry.id, ...entry.data }} client:load />
</BaseLayout>
