---
import BaseLayout from '../layouts/BaseLayout.astro';

const memes = [
  {
    src: '/images/memes/meme1.png',
    alt: 'Prompt Engineering in 2026',
    title: 'Engenharia de Prompt'
  },
  {
    src: '/images/memes/meme2.png',
    alt: 'It works on my machine',
    title: 'Na minha máquina funciona'
  },
  {
    src: '/images/memes/meme3.png',
    alt: 'When the AI realizes it has to maintain legacy code',
    title: 'IA e o código legado'
  },
  {
    src: '/images/memes/meme4.png',
    alt: 'Senior developer saying its a feature not a bug while server room burns',
    title: 'Sênior em dia de deploy'
  },
  {
    src: '/images/memes/meme5.png',
    alt: 'AI brain answering 1+1=3 with a 5 paragraph essay',
    title: 'Alucinação da IA'
  },
  {
    src: '/images/memes/meme6.png',
    alt: 'Developer walking away from explosion after git push force on friday 5PM',
    title: 'Git Push --force na Sexta'
  }
];

const seoDescription = "A maior galeria de memes de TI, programação, inteligência artificial e mundo corporativo tech. Porque rir do código legado é melhor do que chorar.";
---

<BaseLayout 
  title="Humor Tech & Memes de IA | Tech Briefing" 
  description={seoDescription} 
  type="website"
  image="/images/memes/meme1.png"
  tags={["memes TI", "memes programação", "humor dev", "memes inteligência artificial", "piadas programação"]}
>
  <main class="memes-page">
    <header class="memes-hero">
      <div class="container">
        <h1>Humor Tech & IA 🤖</h1>
        <p class="hero-subtitle">
          Um respiro cômico para a comunidade. Porque se a gente não rir do código legado, a gente chora.
        </p>
      </div>
    </header>

    <section class="memes-grid-section">
      <div class="container">
        <div class="masonry-grid">
          {memes.map(meme => (
            <div class="meme-card">
              <img src={meme.src} alt={meme.alt} loading="lazy" />
              <div class="meme-overlay">
                <span>{meme.title}</span>
              </div>
            </div>
          ))}
        </div>
        
        <div class="submit-meme-box">
          <p>Fez um meme bom e quer colocar aqui?</p>
          <a href="/forum" class="btn btn-fill">Mande no Fórum</a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .memes-page {
    padding-bottom: 80px;
  }
  .memes-hero {
    padding: 80px 0 40px;
    text-align: center;
    background: linear-gradient(180deg, var(--bg-2) 0%, transparent 100%);
  }
  .memes-hero h1 {
    margin-bottom: 16px;
    font-size: clamp(32px, 5vw, 48px);
    letter-spacing: -0.02em;
  }
  .hero-subtitle {
    max-width: 600px;
    margin: 0 auto;
    font-size: 18px;
    color: var(--text-muted);
    line-height: 1.6;
  }
  
  .memes-grid-section {
    padding: 40px 0;
  }

  .memes-grid-section .container {
    max-width: 1000px;
  }

  /* Masonry Layout */
  .masonry-grid {
    column-count: 3;
    column-gap: 24px;
    padding-bottom: 60px;
  }
  
  @media (max-width: 900px) {
    .masonry-grid {
      column-count: 2;
    }
  }
  
  @media (max-width: 600px) {
    .masonry-grid {
      column-count: 1;
    }
  }

  .meme-card {
    break-inside: avoid;
    margin-bottom: 24px;
    position: relative;
    border-radius: 16px;
    overflow: hidden;
    background: var(--bg-2);
    border: 1px solid var(--border);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .meme-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0,0,0,0.1);
  }

  .meme-card img {
    width: 100%;
    display: block;
    height: auto;
  }

  .meme-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 24px 20px 16px;
    background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0) 100%);
    color: #fff;
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
  }

  .meme-card:hover .meme-overlay {
    opacity: 1;
  }

  .meme-overlay span {
    font-weight: 600;
    font-size: 15px;
    font-family: var(--sans);
  }

  .submit-meme-box {
    margin: 0 auto;
    max-width: 500px;
    padding: 32px;
    text-align: center;
    border-radius: 16px;
    background: var(--bg-2);
    border: 1px dashed var(--border-hover);
  }

  .submit-meme-box p {
    margin-bottom: 16px;
    font-weight: 500;
    color: var(--text-muted);
  }
</style>
